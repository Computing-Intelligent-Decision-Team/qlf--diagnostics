# Idea

- `target`: 将“索要最近一周可信寄生数据并生成完整压缩包”固化为可移植 Codex skill。
- `success`: 安装后调用 `$analog-harness-parasitic-export` 即可扫描、核验、打包并报告压缩包路径与 SHA-256。
- `boundary`: 不改动源实验，不把 DRC/LVS/PEX 失败数据当作正样本，不在未授权时执行外部上传。
